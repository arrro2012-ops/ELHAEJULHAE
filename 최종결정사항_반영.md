# 최종 결정사항 반영 (6개 의사결정)

**작성일**: 2026-09-13  
**목적**: 사용자 최종 결정사항 정리 및 구현 가이드

---

## 📋 6개 의사결정 최종 결정

### D1: 싱글 모드 "짝꿍 투두" 탭 ✅
```
결정: 표시하지 않음

구현:
- Phase 1: 싱글 모드 → TodoList 탭: "공통 / 내 투두" 2개만 표시
- Phase 2: 커플 모드 연결 후 → "짝꿍 투두" 탭 추가 표시
```

---

### D2: Partner 데이터 구조 ✅
```
결정: 이름/연락처 미추가 (gender, birthDate만)

Partner 최종 구조:
{
  gender: 'female' | 'male'
  birthDate: string
}

👉 이유: 초대 방식이 코드/URL 기반이므로, 개인정보 최소화
```

---

### D3: 커플 D-Day 설정 ✅
```
결정: A(방장) 기준, B는 A의 D-Day에 자동 맞춤

프로세스:
1. B 초대 코드 입력 → 수락 모달
2. 수락 시 B의 D-Day는 자동으로 A의 D-Day 동기화
3. 이후 A 또는 B가 "환경설정"에서 D-Day 변경 시
   → 둘 다의 D-Day 동시 변경 (통일 유지)
   
로직:
- B가 Step 3 D-Day 입력 필요 없음 (자동 적용)
- 또는 입력 후 A와 다르면 A 것으로 덮어쓰기

🎯 목표: 임신준비 부부는 항상 동일한 D-Day 봐야 함
```

---

### D4: Routine 렌더링 방식 (하이브리드) ✅

#### 개요
```
원칙: DB에는 정의만, UI에서 동적 생성
→ DB 최소화 + UI 유연성 최대화
```

#### 데이터 모델

##### 1. Routine (DB)
```typescript
interface Routine {
  id: string
  text: string
  category: 'common' | 'mine' | 'partner'
  repeat: 'daily' | 'weekly' | 'biweekly' | 'monthly'
  time?: string | string[]           // "06:00" 또는 ["06:00", "12:00"]
  startDate: string                   // 루틴 시작 날짜
  endDate?: string                    // 루틴 종료 날짜 (없으면 무한)
  createdAt: string
  deletedAt?: string
}
```

##### 2. 🆕 TodoCompletion (DB - 완료 추적)
```typescript
interface TodoCompletion {
  id: string
  routineId: string                   // 어느 루틴의 완료인지
  date: string                        // 완료 날짜
  completed: boolean
  completedAt?: string
  createdAt: string
}
```

##### 3. Todo (메모리 - 동적 생성)
```typescript
interface Todo {
  id: string                          // 동적 생성: routineId + date 조합
  text: string
  date: string                        // 해당 날짜
  category: 'common' | 'mine' | 'partner'
  completed: boolean                  // TodoCompletion에서 조회
  // ↑ 실제 데이터는 메모리상에만 존재
}
```

#### 렌더링 프로세스 (예: 2026-09-18 ~ 2026-09-25 캘린더 표시)

```
Step 1: Routine 정의 로드 (DB)
  └─ routine_001: "영양제 복용" (daily, 06:00+12:00, startDate=2026-09-01, endDate=무한)
  
Step 2: 캘린더 범위에서 동적 Todo 생성 (메모리)
  └─ 루틴의 repeat 규칙 적용
     repeat='daily' → 2026-09-18, 09-19, ..., 09-25 각각 1개씩 생성
     repeat='weekly' → 2026-09-18, 09-25 (예: 목요일 반복)
     repeat='biweekly' → 이전 주 기준으로 계산
     repeat='monthly' → 월 단위 계산
  
  생성된 Todo (메모리상):
  [
    { id: "routine_001_2026-09-18", text: "영양제 복용", date: "2026-09-18", completed: false },
    { id: "routine_001_2026-09-19", text: "영양제 복용", date: "2026-09-19", completed: false },
    ...
  ]

Step 3: TodoCompletion 확인 (DB)
  └─ DB 쿼리: WHERE routineId='routine_001' AND date BETWEEN '2026-09-18' AND '2026-09-25'
  └─ 완료된 항목 조회:
     [
       { routineId: "routine_001", date: "2026-09-18", completed: true },
       { routineId: "routine_001", date: "2026-09-20", completed: true }
     ]

Step 4: 완료 상태 병합 (UI)
  └─ Step 2의 동적 Todo + Step 3의 completion 상태 병합
  └─ UI 렌더링:
     2026-09-18: [✓] 영양제 복용
     2026-09-19: [ ] 영양제 복용
     2026-09-20: [✓] 영양제 복용
     2026-09-21: [ ] 영양제 복용
     ...
```

#### 사용자 완료 흐름

```
사용자가 [✓] 영양제 복용 (2026-09-18) 클릭
  ↓
1. UI: 메모리 Todo 상태 변경 (즉시 반영)
   { id: "routine_001_2026-09-18", completed: true }
   
2. DB: TodoCompletion 생성/업데이트 (로컬스토리지 또는 Firebase)
   {
     id: "completion_ABC123",
     routineId: "routine_001",
     date: "2026-09-18",
     completed: true,
     completedAt: "2026-09-18T10:30:00Z"
   }
   
3. EXP: +10 (즉시 반영) (F-1)
   Character.currentExp += 10

4. 다음 날 앱 재방문
   → 캐시/localStorage의 Routine 정의 로드
   → TodoCompletion 확인
   → 2026-09-18의 완료 상태 복구 ✅
```

#### 성능 최적화

```
✅ 장점:
- DB 크기 최소화: 루틴 정의만 저장, 개별 일자 데이터 안 저장
- UI 유연성: 범위 변경 시 동적 계산 가능
- 완료 추적: TodoCompletion만 저장 → 저장소 효율

⚠️ 주의:
- repeat 계산 로직 정확성 필수 (weekly, biweekly 경계 처리)
- 대량의 routines가 있을 때 동적 계산 성능 모니터링
- Phase 2 Firebase 동기화 시 TodoCompletion 구독 필수
```

---

### D5: 이벤트 투두 분류 ✅
```
결정: 기본 투두 → 루틴으로 변환 가능

예:
1. D-Day 화면에서 "배란 테스트기 사용" [추가]
   → Todo로 생성 (date: 배란일)
   
2. 사용자가 나중에 "이거 매일 할 건데?" 생각
   → 투두 편집 → "루틴으로 변경" 옵션
   → Routine으로 변환
   {
     text: "배란 테스트기 사용",
     repeat: "daily",
     startDate: 사용자가 지정한 날짜,
     endDate: 배란일 +7일 등
   }

구현:
- Todo 편집 모달에 "[루틴으로 변경]" 버튼 추가
- 클릭 시 루틴 생성 및 원본 todo 삭제
```

---

### D6: 여러 날 투두 (루틴 방식) ✅
```
결정: Routine으로 구현

예: "기초체온 측정" (배란일 -5일 ~ +5일)

구현:
Routine {
  text: "기초체온 측정",
  category: "common",
  repeat: "daily",
  startDate: "2026-09-15" (배란일 -5)
  endDate: "2026-09-25" (배란일 +5)
  time: ["06:30"] (아침 측정)
}

렌더링:
- 2026-09-15 ~ 09-25: 매일 "기초체온 측정" 동적 표시
- 2026-09-26: 루틴 endDate 초과 → 표시 안 됨 ✅
```

---

## 📊 최종 데이터 구조 (3가지 변경)

### 변경 1: 싱글 모드 탭 제거
```
싱글:
  - "공통" / "내 투두" (2개)
  
커플:
  - "공통" / "내 투두" / "짝꿍 투두" (3개)
```

### 변경 2: Partner 구조 유지
```typescript
interface Partner {
  gender: 'female' | 'male'
  birthDate: string
  // (이름/연락처 없음)
}
```

### 변경 3: Routine + TodoCompletion 추가
```typescript
// Routine: DB에 정의만 저장
interface Routine { ... }

// TodoCompletion: 완료 추적 (DB)
interface TodoCompletion { ... }

// Todo: 메모리상 동적 생성 (렌더링용)
interface Todo { ... }
```

---

## 🎯 Phase 1 구현 체크리스트

### 데이터 구조
- [x] Todo (단일 날짜)
- [x] Routine (반복, startDate~endDate) ← 새로 추가
- [x] TodoCompletion (완료 추적) ← 새로 추가
- [x] Character (EXP 시스템)
- [x] User (gender, birthDate, stage, role)
- [x] Partner (gender, birthDate만) ← 간소화
- [x] DDay (D-Day 정의)

### UI/UX
- [ ] OnboardingPage Step 1~3 (Step 4는 Phase 2)
- [ ] MainPage 기본 레이아웃
- [ ] 월 뷰 캘린더 (D-Day 강조)
- [ ] TodoList (공통/내 투두) ← 짝꿍 투두 없음
- [ ] 투두 추가 모달 (월 뷰 날짜 클릭)
- [ ] 투두 완료 (✓ 체크 + EXP)
- [ ] 투두 삭제 (소프트 삭제)

### 로컬 저장소
- [ ] localStorage 투두/루틴/완료 추적
- [ ] 앱 재실행 시 데이터 복구

### 알림 & 커플 (Phase 2+)
- [ ] Phase 2: Firebase 연동
- [ ] Phase 3: 실시간 알림 & 동기화

---

## ✅ 최종 준비 상태

### 지식정보.md 업데이트 필요
- [x] 투두 vs 루틴 구분 명확화
- [x] TodoCompletion 엔티티 추가
- [x] 하이브리드 렌더링 방식 설명
- [x] D3 결정사항 (커플 D-Day 통일)

### 상세기획서.md 보완 필요
- [x] 투두/루틴 분리 명시
- [x] D-Day 루틴 기본값 설명
- [ ] 최종 데이터 모델 추가 (선택)

### 구현 준비
- [x] 데이터 구조 확정
- [x] 렌더링 알고리즘 정의 (D4 하이브리드)
- [x] 모드별 UI 차이 명시 (D1)
- [x] 이벤트 투두 변환 로직 (D5)

**상태**: 🟢 **모든 의사결정 확정, 구현 준비 완료!**

